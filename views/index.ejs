<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>EJS Template Exercise</title>
</head>
<body>
    <!-- Activity 4: Insert code here to include the header (found in the partials folder) to your page -->
    <%- include ('partials/header.ejs') %>
    <h1>Hello <%= name %></h1>
    <!-- Activity 1: Insert code here to display “You are age years old” where age is the number passed in from app.js. -->
    You are <%= age %> years old.

    <!-- Activity 2: Insert code here to display statement based on age variable -->
    <% if (age >= 18) { %>
        <p> You are an adult. </p>
    <% } else if (age > 12) { %>
        <p> You are a teenager. </p>
    <% } else { %>
        <p> You are a child.</p>
    <% } %>
</body>
</html>